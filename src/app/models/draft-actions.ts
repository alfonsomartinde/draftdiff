export enum DraftAction {
  HYDRATE = 'draft/hydrate',
  TICK = 'draft/tick',
  READY = 'draft/ready',
  SET_TEAM_NAME = 'draft/set-team-name',
  SELECT = 'draft/select',
  CONFIRM = 'draft/confirm',
}
